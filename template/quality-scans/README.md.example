# Quality Scans(质量扫描报告)

由 `boss-agent` 的 SCAN 阶段产出;每个扫描一个子目录:

```
quality-scans/
└── <YYYYMMDD>-<scope>/           # scope: docs | frontend | backend | db | data | all
    ├── scan-report.md            # ★ 结构化扫描报告(boss 审批用)
    └── scan-stdout.log           # headless scanner 的 stdout
```

触发方式:`/boss-agent 扫描全库` 或 `/boss-agent 只扫描<文档|前端|后端|数据库|数据>`。
审批:技术项 → 拆 fix 批次派 worker;改现有 UI 设计/Env-only/数据口径 → 记 deferred-notes。

报告结构(由 boss-scanner 定义):摘要(规模/严重度/类别)→ 发现清单(按严重度排序,带 file:line)
→ 发现详情(top 15-20)→ 建议修复批次。
